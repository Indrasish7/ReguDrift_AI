steps:
  # ==========================================================
  # 1. Build and push FastAPI Backend container image
  # ==========================================================
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/web:latest'
      - '.'
      - '-f'
      - 'Dockerfile'
    id: 'build-web'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/web:latest'
    id: 'push-web'
    waitFor: ['build-web']

  # ==========================================================
  # 2. Build and push Next.js Frontend container image
  # ==========================================================
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/frontend:latest'
      - './frontend'
      - '-f'
      - './frontend/Dockerfile'
    id: 'build-frontend'
    waitFor: ['-']

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/frontend:latest'
    id: 'push-frontend'
    waitFor: ['build-frontend']

  # ==========================================================
  # 3. Deploy services concurrently to Cloud Run
  # ==========================================================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'regudrift-web'
      - '--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/web:latest'
      - '--region=${_REGION}'
    id: 'deploy-web'
    waitFor: ['push-web']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'regudrift-frontend'
      - '--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/frontend:latest'
      - '--region=${_REGION}'
    id: 'deploy-frontend'
    waitFor: ['push-frontend', 'deploy-web']


images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/web:latest'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/regudrift-repo/frontend:latest'

substitutions:
  _REGION: 'asia-south1'

options:
  logging: 'CLOUD_LOGGING_ONLY'
